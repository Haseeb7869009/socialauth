import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oxxopost.app',
  appName: 'Oxxo Post',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
